# 🧩 VS Code Setup — React Native + TypeScript

Configuração leve e produtiva para desenvolvimento com **React Native**, **Expo**, **TypeScript** e **TailwindCSS**.

- adicione a pasta .vscode no seu projeto, e reinicie o editor para que ele sugira as alterações.
ou digite `ctrl + shift + p`, procure pelo user settings (JSON) e cole as configurações para que o editor as use em todos os projetos.
